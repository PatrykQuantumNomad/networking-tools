---
phase: 31-remaining-tool-skills
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/dig/SKILL.md
  - .claude/skills/curl/SKILL.md
  - .claude/skills/gobuster/SKILL.md
  - .claude/skills/ffuf/SKILL.md
autonomous: true
requirements: [TOOL-11, TOOL-12, TOOL-15, TOOL-16]

must_haves:
  truths:
    - "User can invoke dig skill and see references to query-dns-records, attempt-zone-transfer, check-dns-propagation, and examples scripts"
    - "User can invoke curl skill and see references to check-ssl-certificate, debug-http-response, test-http-endpoints, and examples scripts"
    - "User can invoke gobuster skill and see references to discover-directories, enumerate-subdomains, and examples scripts"
    - "User can invoke ffuf skill and see references to fuzz-parameters and examples scripts"
    - "All 4 skills have disable-model-invocation: true in frontmatter"
    - "dig skill documents domain argument (not IP/URL)"
    - "gobuster and ffuf skills document optional wordlist second argument"
  artifacts:
    - path: ".claude/skills/dig/SKILL.md"
      provides: "Dig tool skill with DNS records, zone transfers, and propagation check categories"
      contains: "disable-model-invocation: true"
    - path: ".claude/skills/curl/SKILL.md"
      provides: "Curl tool skill with SSL inspection, HTTP debugging, and endpoint testing categories"
      contains: "disable-model-invocation: true"
    - path: ".claude/skills/gobuster/SKILL.md"
      provides: "Gobuster tool skill with directory discovery and subdomain enumeration categories"
      contains: "disable-model-invocation: true"
    - path: ".claude/skills/ffuf/SKILL.md"
      provides: "Ffuf tool skill with parameter fuzzing category"
      contains: "disable-model-invocation: true"
  key_links:
    - from: ".claude/skills/dig/SKILL.md"
      to: "scripts/dig/*.sh"
      via: "script reference paths in Available Scripts section"
      pattern: "bash scripts/dig/"
    - from: ".claude/skills/curl/SKILL.md"
      to: "scripts/curl/*.sh"
      via: "script reference paths in Available Scripts section"
      pattern: "bash scripts/curl/"
    - from: ".claude/skills/gobuster/SKILL.md"
      to: "scripts/gobuster/*.sh"
      via: "script reference paths in Available Scripts section"
      pattern: "bash scripts/gobuster/"
    - from: ".claude/skills/ffuf/SKILL.md"
      to: "scripts/ffuf/*.sh"
      via: "script reference paths in Available Scripts section"
      pattern: "bash scripts/ffuf/"
---

<objective>
Create Claude Code skill files for 4 web/DNS reconnaissance tools: dig, curl, gobuster, and ffuf.

Purpose: These tools are standard web and DNS recon tools that share similar argument patterns (domains, URLs, optional wordlists). Grouping them together keeps the executor focused on one tool category.

Output: 4 SKILL.md files in `.claude/skills/{dig,curl,gobuster,ffuf}/SKILL.md`
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-remaining-tool-skills/31-RESEARCH.md
@.claude/skills/nmap/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dig and curl skills</name>
  <files>.claude/skills/dig/SKILL.md, .claude/skills/curl/SKILL.md</files>
  <action>
Create SKILL.md for dig and curl following the exact validated pattern from `.claude/skills/nmap/SKILL.md`.

**dig** (`.claude/skills/dig/SKILL.md`):
- Frontmatter: name: dig, description: "DNS record querying and zone transfer testing using dig wrapper scripts", disable-model-invocation: true
- H1: "Dig DNS Tool"
- Purpose line: "Run dig wrapper scripts for DNS record queries, zone transfer testing, and propagation checks."
- Available Scripts categories:
  - **DNS Records**: `bash scripts/dig/query-dns-records.sh [domain] [-j] [-x]` -- Query A, AAAA, MX, NS, TXT, SOA, and CNAME records for a domain
  - **Zone Transfers**: `bash scripts/dig/attempt-zone-transfer.sh [domain] [-j] [-x]` -- Attempt AXFR zone transfer against a domain's nameservers
  - **Propagation Checks**: `bash scripts/dig/check-dns-propagation.sh [domain] [-j] [-x]` -- Check DNS propagation across multiple public resolvers
  - **Learning Mode**: `bash scripts/dig/examples.sh <target>` -- View 10 common dig patterns with explanations
- Flags section: identical verbatim to nmap skill
- Defaults: Domain defaults to `example.com` when not provided; Argument is a domain name (not IP address or URL); Additional script flags: `-v`/`--verbose`, `-q`/`--quiet`; Scripts display commands without running them unless `-x` is passed
- Target Validation: standard network tool section (same as nmap)

**curl** (`.claude/skills/curl/SKILL.md`):
- Frontmatter: name: curl, description: "HTTP request debugging and SSL inspection using curl wrapper scripts", disable-model-invocation: true
- H1: "Curl HTTP Tool"
- Purpose line: "Run curl wrapper scripts for SSL certificate inspection, HTTP response debugging, and endpoint testing."
- Available Scripts categories:
  - **SSL/TLS Inspection**: `bash scripts/curl/check-ssl-certificate.sh [target] [-j] [-x]` -- Inspect SSL/TLS certificates for expiry, chain validity, and cipher suites
  - **HTTP Debugging**: `bash scripts/curl/debug-http-response.sh [target] [-j] [-x]` -- Debug HTTP responses including headers, redirects, and timing
  - **Endpoint Testing**: `bash scripts/curl/test-http-endpoints.sh [target] [-j] [-x]` -- Test HTTP endpoints for status codes, response times, and content validation
  - **Learning Mode**: `bash scripts/curl/examples.sh <target>` -- View 10 common curl patterns with explanations
- Flags section: identical verbatim
- Defaults: Target defaults to `example.com` (SSL) or `https://example.com` (HTTP scripts); Target can be a domain name or full URL; Additional script flags: `-v`/`--verbose`, `-q`/`--quiet`; Scripts display commands without running them unless `-x` is passed
- Target Validation: standard network tool section (same as nmap)

Use double-dash `--` for list descriptions. Keep each skill under 55 lines.
  </action>
  <verify>
Verify both files exist and have correct structure:
- `head -3 .claude/skills/dig/SKILL.md` shows YAML frontmatter with disable-model-invocation: true
- `head -3 .claude/skills/curl/SKILL.md` shows YAML frontmatter with disable-model-invocation: true
- `grep -c 'bash scripts/dig/' .claude/skills/dig/SKILL.md` returns 4 (one per script)
- `grep -c 'bash scripts/curl/' .claude/skills/curl/SKILL.md` returns 4 (one per script)
- `grep 'domain name' .claude/skills/dig/SKILL.md` confirms domain argument documentation
  </verify>
  <done>dig and curl SKILL.md files exist with accurate arguments (domain for dig, domain/URL for curl), additional flags documented, and standard Target Validation</done>
</task>

<task type="auto">
  <name>Task 2: Create gobuster and ffuf skills</name>
  <files>.claude/skills/gobuster/SKILL.md, .claude/skills/ffuf/SKILL.md</files>
  <action>
Create SKILL.md for gobuster and ffuf following the exact validated pattern.

**gobuster** (`.claude/skills/gobuster/SKILL.md`):
- Frontmatter: name: gobuster, description: "Directory and subdomain brute-forcing using gobuster wrapper scripts", disable-model-invocation: true
- H1: "Gobuster Directory Scanner"
- Purpose line: "Run gobuster wrapper scripts for directory discovery and subdomain enumeration with educational examples."
- Available Scripts categories:
  - **Directory Discovery**: `bash scripts/gobuster/discover-directories.sh [target] [wordlist] [-j] [-x]` -- Discover hidden directories and files on web servers
  - **Subdomain Enumeration**: `bash scripts/gobuster/enumerate-subdomains.sh [domain] [wordlist] [-j] [-x]` -- Enumerate subdomains using DNS brute-forcing
  - **Learning Mode**: `bash scripts/gobuster/examples.sh <target>` -- View 10 common gobuster patterns with explanations
- Flags section: identical verbatim
- Defaults: discover-directories defaults to `http://localhost:8080` (DVWA); enumerate-subdomains defaults to `example.com`; Second argument is an optional custom wordlist path; Scripts display commands without running them unless `-x` is passed
- Target Validation: standard network tool section (same as nmap)

**ffuf** (`.claude/skills/ffuf/SKILL.md`):
- Frontmatter: name: ffuf, description: "Web fuzzing for parameters, directories, and endpoints using ffuf wrapper scripts", disable-model-invocation: true
- H1: "Ffuf Web Fuzzer"
- Purpose line: "Run ffuf wrapper scripts for web parameter fuzzing with educational examples and structured JSON output."
- Available Scripts categories:
  - **Parameter Fuzzing**: `bash scripts/ffuf/fuzz-parameters.sh [target] [wordlist] [-j] [-x]` -- Fuzz URL parameters, headers, and POST data to discover hidden inputs
  - **Learning Mode**: `bash scripts/ffuf/examples.sh <target>` -- View 10 common ffuf patterns with explanations
- Flags section: identical verbatim
- Defaults: Target defaults to `http://localhost:8080` (DVWA); Second argument is an optional custom wordlist path; Scripts display commands without running them unless `-x` is passed
- Target Validation: standard network tool section (same as nmap)

NOTE: ffuf has the smallest skill (only 1 use-case script + examples). This is expected and correct -- do not pad with extra content.

Use double-dash `--` for list descriptions. Keep gobuster under 50 lines, ffuf under 45 lines.
  </action>
  <verify>
Verify both files exist and have correct structure:
- `head -3 .claude/skills/gobuster/SKILL.md` shows YAML frontmatter with disable-model-invocation: true
- `head -3 .claude/skills/ffuf/SKILL.md` shows YAML frontmatter with disable-model-invocation: true
- `grep -c 'bash scripts/gobuster/' .claude/skills/gobuster/SKILL.md` returns 3 (one per script)
- `grep -c 'bash scripts/ffuf/' .claude/skills/ffuf/SKILL.md` returns 2 (one per script)
- `grep 'wordlist' .claude/skills/gobuster/SKILL.md` confirms optional wordlist argument documented
- `grep 'wordlist' .claude/skills/ffuf/SKILL.md` confirms optional wordlist argument documented
  </verify>
  <done>gobuster and ffuf SKILL.md files exist with accurate arguments including optional wordlist second argument, correct script counts (3 for gobuster, 2 for ffuf), and standard Target Validation</done>
</task>

</tasks>

<verification>
- All 4 files exist: `.claude/skills/{dig,curl,gobuster,ffuf}/SKILL.md`
- All 4 have `disable-model-invocation: true` in frontmatter
- Each skill references the correct scripts with accurate arguments
- dig documents domain argument (not IP/URL)
- curl documents domain or URL as target
- gobuster and ffuf document optional second wordlist argument
- ffuf correctly has only 2 script references (smallest skill)
- All skills use standard Target Validation section (network tools)
</verification>

<success_criteria>
- 4 SKILL.md files created for web/DNS recon tools
- Each skill accurately documents per-script arguments including optional wordlist
- All use disable-model-invocation: true
- Pattern matches validated Phase 29 template
</success_criteria>

<output>
After completion, create `.planning/phases/31-remaining-tool-skills/31-03-SUMMARY.md`
</output>
