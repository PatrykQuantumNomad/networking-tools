---
phase: 38-agent-personas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-agent-personas.bats
  - .claude/agents/pentester.md
  - .claude/skills/pentest-conventions/SKILL.md
  - .claude/skills/pentester/SKILL.md
  - netsec-skills/agents/pentester.md
  - netsec-skills/skills/agents/pentester/SKILL.md
  - netsec-skills/skills/utility/pentest-conventions/SKILL.md
autonomous: true
requirements: [AGEN-01, AGEN-02]

must_haves:
  truths:
    - "BATS tests validate agent file structure, skill references, invoker fields, and sync for all 3 agents"
    - "Pentester agent body has dual-mode execution rules (wrapper scripts when available, direct commands when standalone)"
    - "pentest-conventions skill exists in plugin as a real file with dual-mode awareness"
    - "Pentester invoker skill is a real file in plugin with context: fork and agent: field"
    - "Plugin pentester agent and invoker are identical to in-repo versions"
  artifacts:
    - path: "tests/test-agent-personas.bats"
      provides: "Structural validation for all 3 agents, 3 invokers, pentest-conventions, and sync"
      min_lines: 100
    - path: ".claude/agents/pentester.md"
      provides: "Pentester agent with dual-mode execution rules"
      contains: "If standalone"
    - path: ".claude/skills/pentest-conventions/SKILL.md"
      provides: "Pentesting conventions with dual-mode awareness"
      contains: "standalone"
    - path: "netsec-skills/agents/pentester.md"
      provides: "Plugin copy of pentester agent (real file, not symlink)"
    - path: "netsec-skills/skills/agents/pentester/SKILL.md"
      provides: "Plugin copy of pentester invoker (real file, not symlink)"
    - path: "netsec-skills/skills/utility/pentest-conventions/SKILL.md"
      provides: "Plugin copy of pentest-conventions (new, never existed before)"
  key_links:
    - from: "tests/test-agent-personas.bats"
      to: ".claude/agents/*.md"
      via: "BATS assertions checking frontmatter fields and body content"
      pattern: "AGEN-01"
    - from: ".claude/agents/pentester.md"
      to: "netsec-skills/agents/pentester.md"
      via: "cp (real file copy)"
      pattern: "cmp -s"
    - from: ".claude/skills/pentester/SKILL.md"
      to: "netsec-skills/skills/agents/pentester/SKILL.md"
      via: "cp (real file copy)"
      pattern: "cmp -s"
---

<objective>
Create BATS test scaffold for all 3 agent personas and 3 invoker skills, then pilot the dual-mode transformation on the pentester agent (the most complex agent with 5 workflow skill preloads and in-repo-only execution rules) plus pentest-conventions (missing from plugin entirely, referenced by all 3 agents).

Purpose: Validate the agent transformation pattern -- dual-mode agent body, pentest-conventions portability, symlink replacement, and skill reference resolution -- on the hardest case before scaling to defender and analyst. This mirrors the pilot-then-scale approach from Phases 36 and 37.

Output: BATS test file covering AGEN-01 and AGEN-02 for all 3 agents, plus pentester agent + invoker as real files in plugin, plus pentest-conventions as a new real file in plugin.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-agent-personas/38-RESEARCH.md
@.planning/phases/37-standalone-workflow-skills/37-01-SUMMARY.md
@tests/test-dual-mode-skills.bats
@tests/test-workflow-skills.bats
</context>

<interfaces>
<!-- Current in-repo agent and skill files that will be transformed -->

From .claude/agents/pentester.md:
```yaml
name: pentester
tools: Read, Grep, Glob, Bash
model: inherit
memory: project
skills:
  - pentest-conventions
  - recon
  - scan
  - fuzz
  - crack
  - sniff
```
Body contains: "Always use wrapper scripts with -j -x flags" and "Never invoke raw security tools directly"

From .claude/skills/pentester/SKILL.md:
```yaml
name: pentester
context: fork
agent: pentester
disable-model-invocation: true
argument-hint: "<target-or-task>"
```

From .claude/skills/pentest-conventions/SKILL.md:
```yaml
name: pentest-conventions
user-invocable: false
```
Body contains: in-repo references to scripts/, .claude/hooks/, make lab-up

From tests/test-dual-mode-skills.bats (BATS pattern):
- setup() with load 'test_helper/common-setup' + _common_setup
- _extract_description() awk helper for frontmatter
- Arrays for iteration, failing/missing collection, fail at end
- _section_content() awk helper for section extraction

From tests/test-workflow-skills.bats (additional patterns):
- WORKFLOWS array iteration
- cmp -s for sync validation
- Symlink detection: [[ -L "$file" ]]
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create BATS test scaffold for agent persona validation</name>
  <files>tests/test-agent-personas.bats</files>
  <action>
Create `tests/test-agent-personas.bats` following the exact pattern from `tests/test-dual-mode-skills.bats` and `tests/test-workflow-skills.bats` (same setup(), same awk helpers).

Define arrays:
- `AGENTS=(pentester defender analyst)`
- `INVOKERS=(pentester defender analyst)` (same names, different paths)

Reuse `_extract_description()` awk helper from existing tests.

Write these test blocks:

**AGEN-01: Agent file structural tests**

1. `AGEN-01: each agent file exists as real file in plugin` -- For each agent in AGENTS, check that `netsec-skills/agents/${agent}.md` exists AND is NOT a symlink (`[[ -L "$file" ]]` should be false). Collect failures.

2. `AGEN-01: each agent has skills: field in frontmatter` -- For each agent, grep for `^skills:` in `$PROJECT_ROOT/.claude/agents/${agent}.md`. Collect failures.

3. `AGEN-01: each agent skills: reference resolves to a plugin skill` -- For each agent, extract the skills listed under `skills:` (lines matching `^  - ` after `^skills:` line, up to the next non-indented line or `---`). For each skill name, verify that a SKILL.md exists somewhere in `netsec-skills/skills/` (search `netsec-skills/skills/*/SKILL.md` and `netsec-skills/skills/*/*/SKILL.md` for a file whose frontmatter `name:` field matches). Use a find + grep approach: `find "$PROJECT_ROOT/netsec-skills/skills" -name SKILL.md -exec grep -l "^name: ${skill_name}$" {} \;`. Collect failures as "agent:skill_name (not found in plugin)".

4. `AGEN-01: agent body does not contain in-repo-only instructions` -- For each agent, extract body content after the closing `---` frontmatter delimiter. Grep for the literal strings "Never invoke raw" and "Always use wrapper" (case-insensitive). If either is found, collect as failure. These phrases are in-repo-only and wrong for standalone mode.

5. `AGEN-01: pentester agent has dual-mode execution rules` -- Check that `$PROJECT_ROOT/.claude/agents/pentester.md` contains both "wrapper scripts" AND "standalone" (or "direct") in its body, indicating dual-mode awareness. This is pentester-specific because defender and analyst do not execute tools.

**AGEN-02: Invoker skill structural tests**

6. `AGEN-02: each invoker skill exists as real file in plugin` -- For each invoker in INVOKERS, check that `netsec-skills/skills/agents/${invoker}/SKILL.md` exists AND the directory `netsec-skills/skills/agents/${invoker}` is NOT a symlink. Collect failures.

7. `AGEN-02: each invoker skill has context: fork and agent: fields` -- For each invoker, grep for `^context: fork` and `^agent:` in `$PROJECT_ROOT/.claude/skills/${invoker}/SKILL.md`. Collect failures.

8. `AGEN-02: pentest-conventions exists in plugin as real file` -- Check that `netsec-skills/skills/utility/pentest-conventions/SKILL.md` exists and the directory is NOT a symlink. Single assertion, not a loop.

**SYNC tests**

9. `SYNC: in-repo and plugin agent files are identical` -- For each agent, `cmp -s` between `$PROJECT_ROOT/.claude/agents/${agent}.md` and `$PROJECT_ROOT/netsec-skills/agents/${agent}.md`. Collect failures.

10. `SYNC: in-repo and plugin invoker skills are identical` -- For each invoker, `cmp -s` between `$PROJECT_ROOT/.claude/skills/${invoker}/SKILL.md` and `$PROJECT_ROOT/netsec-skills/skills/agents/${invoker}/SKILL.md`. Collect failures.

11. `SYNC: in-repo and plugin pentest-conventions are identical` -- `cmp -s` between `$PROJECT_ROOT/.claude/skills/pentest-conventions/SKILL.md` and `$PROJECT_ROOT/netsec-skills/skills/utility/pentest-conventions/SKILL.md`. Single assertion.

**Utility skill tests**

12. `AGEN-01: report skill exists as real file in plugin` -- Check that `netsec-skills/skills/utility/report/SKILL.md` exists and the directory is NOT a symlink. (Required by analyst agent's skills: field.)

13. `SYNC: in-repo and plugin report skills are identical` -- `cmp -s` between the two report SKILL.md files.

14. `SYNC: no remaining symlinks in plugin directory` -- Run `find "$PROJECT_ROOT/netsec-skills" -type l` and check that output is empty. If symlinks remain, list them in the failure message.

Use the same failure reporting pattern as existing tests: collect into `failing` or `missing` arrays, then `fail` with descriptive message at the end.

IMPORTANT: Do NOT check check-tools in AGEN-01 tests -- it is not referenced by any agent's skills: field. It will be handled as opportunistic cleanup in Plan 02, and the "no remaining symlinks" test will catch it.
  </action>
  <verify>
    <automated>./tests/bats/bin/bats tests/test-agent-personas.bats --timing 2>&1 | tail -30</automated>
  </verify>
  <done>BATS test file exists with 14 tests covering AGEN-01 (agent files, skills references, dual-mode body), AGEN-02 (invoker files, context/agent fields, pentest-conventions), SYNC (agent sync, invoker sync, conventions sync, report sync, no symlinks). Currently, most tests fail because symlinks have not been replaced yet -- that is expected and will be fixed in Task 2 and Plan 02.</done>
</task>

<task type="auto">
  <name>Task 2: Pilot pentester agent + pentest-conventions dual-mode transformation</name>
  <files>.claude/agents/pentester.md, .claude/skills/pentest-conventions/SKILL.md, .claude/skills/pentester/SKILL.md, netsec-skills/agents/pentester.md, netsec-skills/skills/agents/pentester/SKILL.md, netsec-skills/skills/utility/pentest-conventions/SKILL.md</files>
  <action>
**Step 1: Transform pentest-conventions to dual-mode**

Update `.claude/skills/pentest-conventions/SKILL.md` to work in both in-repo and standalone contexts. Keep the frontmatter EXACTLY as-is (`name: pentest-conventions`, `user-invocable: false`). Rewrite the body with dual-mode awareness:

```markdown
# Pentesting Conventions

Background reference for pentesting workflows. Covers target notation, output formats, safety architecture, scope configuration, lab targets, and project structure.

## Target Notation

- **IP addresses** -- Single host (10.0.0.1) or CIDR range (192.168.1.0/24)
- **Hostnames** -- Fully qualified (example.com) or short names
- **URLs** -- Full URL format (http://localhost:8080/path)
- **Localhost equivalents** -- `localhost` and `127.0.0.1` are interchangeable
- **Lab targets** -- Always use `localhost` paired to the correct port (see Lab Targets below)

## Output Expectations

**If wrapper scripts are available** (in-repo mode):

Use-case scripts (`scripts/<tool>/<use-case>.sh`) support two flags:

- `-j` / `--json` -- Output structured JSON envelope (enables PostToolUse hook summary)
- `-x` / `--execute` -- Execute commands instead of displaying them

Always add `-j` when running scripts so Claude receives parsed JSON via the PostToolUse hook. Without `-j`, Claude gets raw terminal output that is harder to parse.

The `examples.sh` scripts are learning-mode only and do NOT support `-j` or `-x`.

**If standalone** (plugin mode):

Invoke security tools directly using the command knowledge from your preloaded skills. There are no wrapper scripts -- run tool commands directly and review output manually.

## Safety Rules

- All commands should be validated against `.pentest/scope.json` before execution
- Check scope before every tool execution
- Run security tools responsibly and only against authorized targets
- If a scope file does not exist, create one with `/netsec-scope init` or manually create `.pentest/scope.json` with `{"targets": ["localhost", "127.0.0.1"]}`

## Scope File

Targets must be listed in `.pentest/scope.json` before any pentesting commands will execute.

Format:

\`\`\`json
{"targets": ["localhost", "127.0.0.1"]}
\`\`\`

- The scope file is gitignored (lives in `.pentest/` directory)
- Add targets only after confirming authorization to test them
- Default safe targets: localhost, 127.0.0.1

## Lab Targets

Docker-based vulnerable applications for safe practice:

| Service | URL | Credentials |
|---------|-----|-------------|
| DVWA | http://localhost:8080 | admin / password |
| Juice Shop | http://localhost:3030 | (register an account) |
| WebGoat | http://localhost:8888/WebGoat | (register an account) |
| VulnerableApp | http://localhost:8180/VulnerableApp | -- |

If lab infrastructure is available (in-repo mode), manage containers via make targets:

- `make lab-up` -- Start all containers
- `make lab-down` -- Stop all containers
- `make lab-status` -- Show container status

In standalone mode, set up your own vulnerable targets or use existing lab infrastructure.

## Project Structure

- `scripts/<tool>/examples.sh` -- Educational examples (10 per tool, learning mode only) [in-repo only]
- `scripts/<tool>/<use-case>.sh` -- Task-focused scripts supporting `-j` and `-x` flags [in-repo only]
- `scripts/common.sh` -- Shared utilities (colors, require_cmd, safety_banner, check_cmd) [in-repo only]
- `scripts/check-tools.sh` -- Detects which of 18 pentesting tools are installed [in-repo only]
- `labs/docker-compose.yml` -- Vulnerable Docker targets for practice [in-repo only]
- `.pentest/scope.json` -- Target allowlist (gitignored)
- `.pentest/audit.log` -- Audit log of all tool invocations (gitignored)
```

Note: Escape the backtick code fence for JSON with backslash in the write (the markdown should render as a normal code block). Keep the file around 80 lines. The key change: conditional language for wrapper scripts vs standalone, and `[in-repo only]` markers on paths that only exist in the repo.

**Step 2: Transform pentester agent body to dual-mode**

Update `.claude/agents/pentester.md`. Keep frontmatter EXACTLY as-is (name, description, tools, model, memory, skills -- all unchanged). Replace the body with dual-mode execution rules:

```markdown
You are a senior penetration tester conducting authorized security assessments.

When invoked with a target:
1. Verify the target is in scope (read .pentest/scope.json)
2. Plan the engagement based on the target type (IP, URL, domain, hash file)
3. Execute the appropriate workflow using the preloaded skill instructions
4. Adapt your approach based on findings from each step
5. Provide a structured summary of all findings when complete

## Execution Rules

Detect which mode you are operating in:
- **If wrapper scripts are available** (check `test -f scripts/nmap/identify-ports.sh`): Use wrapper scripts with `-j -x` flags: `bash scripts/<tool>/<script>.sh <target> -j -x`
- **If standalone:** Invoke security tools directly using the command knowledge from your preloaded workflow skills

In either mode:
- Reference the preloaded workflow skills for step-by-step instructions
- If a tool is not installed, skip that step and note it
- Check scope before every tool execution

## Workflow Selection

Based on the target and task, select the appropriate workflow(s):
- Network targets (IPs, domains): Start with recon, then scan
- Web applications (URLs): Start with scan, then fuzz
- Hash files: Use crack workflow
- Network interfaces or pcap files: Use sniff workflow
- Combine workflows as needed for comprehensive assessments

## Output Style

Provide verbose, detailed output during execution. When complete, deliver:
- Executive summary (2-3 sentences)
- Findings organized by severity (Critical/High/Medium/Low)
- Specific evidence for each finding
- Recommended next steps

Update your agent memory with effective attack paths and tool combinations.

## Important

Do NOT try to invoke /recon, /scan, /fuzz, /crack, or /sniff as slash commands. The skill content is preloaded as reference instructions -- read the instructions and execute the bash commands directly.
```

The key changes: "Execution Rules" section now has dual-mode detection with `test -f` check, replacing the hard-coded "Always use wrapper scripts" / "Never invoke raw" instructions.

**Step 3: Replace pentester symlinks with real files in plugin**

```bash
# Agent file
rm -f netsec-skills/agents/pentester.md
cp .claude/agents/pentester.md netsec-skills/agents/pentester.md

# Invoker skill
rm -f netsec-skills/skills/agents/pentester
mkdir -p netsec-skills/skills/agents/pentester
cp .claude/skills/pentester/SKILL.md netsec-skills/skills/agents/pentester/SKILL.md

# pentest-conventions (NEW -- never existed in plugin)
mkdir -p netsec-skills/skills/utility/pentest-conventions
cp .claude/skills/pentest-conventions/SKILL.md netsec-skills/skills/utility/pentest-conventions/SKILL.md
```

**Step 4: Verify sync**

Run `cmp -s` between in-repo and plugin versions for all 3 files (pentester agent, pentester invoker, pentest-conventions). They must be identical.

**Step 5: Run BATS tests**

Run `./tests/bats/bin/bats tests/test-agent-personas.bats --timing` and verify that pentester-related tests pass. Defender and analyst tests will still fail as expected (they are Plan 02 scope).

CRITICAL things to NOT do:
- Do NOT change the pentester agent frontmatter (name, description, tools, model, memory, skills fields stay identical)
- Do NOT change the pentester invoker SKILL.md content (it is already correct for both contexts)
- Do NOT remove pentest-conventions from the in-repo version or change its name/description fields
- Do NOT add pentest-conventions to marketplace.json (it has `user-invocable: false` and should not be listed)
- Do NOT touch defender or analyst files (those are Plan 02 scope)
  </action>
  <verify>
    <automated>./tests/bats/bin/bats tests/test-agent-personas.bats --timing 2>&1 | tail -30</automated>
  </verify>
  <done>Pentester agent has dual-mode execution rules (no "Never invoke raw" or "Always use wrapper" phrases). pentest-conventions exists in plugin as real file with dual-mode awareness. Pentester invoker is a real file (not symlink) in plugin. All 3 new/modified plugin files are identical to in-repo versions (cmp -s passes). BATS tests pass for pentester; defender and analyst tests still fail as expected (Plan 02 scope).</done>
</task>

</tasks>

<verification>
1. `./tests/bats/bin/bats tests/test-agent-personas.bats --timing` -- pentester-related tests pass; defender/analyst expected failures
2. `cmp -s .claude/agents/pentester.md netsec-skills/agents/pentester.md && echo MATCH` -- plugin agent is identical
3. `cmp -s .claude/skills/pentester/SKILL.md netsec-skills/skills/agents/pentester/SKILL.md && echo MATCH` -- plugin invoker is identical
4. `cmp -s .claude/skills/pentest-conventions/SKILL.md netsec-skills/skills/utility/pentest-conventions/SKILL.md && echo MATCH` -- plugin conventions is identical
5. `test ! -L netsec-skills/agents/pentester.md && echo REAL` -- not a symlink
6. `test ! -L netsec-skills/skills/agents/pentester && echo REAL` -- not a symlink
7. `test -d netsec-skills/skills/utility/pentest-conventions && echo EXISTS` -- new directory exists
8. `grep -c "If standalone" .claude/agents/pentester.md` -- should return at least 1
9. `grep -c "Never invoke raw" .claude/agents/pentester.md` -- should return 0
10. `grep -c "standalone" .claude/skills/pentest-conventions/SKILL.md` -- should return at least 1
</verification>

<success_criteria>
- BATS test file exists with 14 tests covering all AGEN requirements
- Pentester agent has dual-mode execution rules with test -f detection
- pentest-conventions skill has dual-mode awareness with conditional language
- All 3 pentester-related plugin files are real (not symlinks) and identical to in-repo
- pentest-conventions exists in plugin for the first time (was completely missing)
- No in-repo-only instructions ("Never invoke raw", "Always use wrapper") in pentester agent
- Defender and analyst remain untouched (Plan 02 scope)
</success_criteria>

<output>
After completion, create `.planning/phases/38-agent-personas/38-01-SUMMARY.md`
</output>
