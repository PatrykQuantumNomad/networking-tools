---
phase: 39-end-to-end-testing-and-publication
plan: 02
type: execute
wave: 2
depends_on: [39-01]
files_modified: []
autonomous: false
requirements: [PLUG-04, PUBL-01, PUBL-02]

must_haves:
  truths:
    - "Full BATS test suite passes with zero regressions"
    - "E2E publication validation script passes all checks"
    - "Plugin loads successfully via claude --plugin-dir ./netsec-skills"
    - "Plugin skills are discoverable and respond to triggers after loading"
  artifacts:
    - path: "scripts/test-e2e-publication.sh"
      provides: "E2E validation (created in 39-01)"
    - path: ".claude-plugin/marketplace.json"
      provides: "Repo-root marketplace catalog (created in 39-01)"
  key_links:
    - from: "claude --plugin-dir ./netsec-skills"
      to: "netsec-skills/.claude-plugin/plugin.json"
      via: "Plugin loader reads manifest"
      pattern: "plugin.json"
    - from: ".claude-plugin/marketplace.json"
      to: "netsec-skills/"
      via: "source field"
      pattern: "netsec-skills"
---

<objective>
Run full validation suite and smoke-test the plugin with a real Claude Code session to confirm publication readiness.

Purpose: Plan 01 creates the artifacts. This plan verifies everything works end-to-end -- automated tests pass AND the plugin actually loads in Claude Code. This is the final gate before pushing to GitHub for publication.

Output: Validated, publication-ready plugin with confirmed test results
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-end-to-end-testing-and-publication/39-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated validation suite</name>
  <files></files>
  <action>
Run the complete automated validation suite in sequence:

1. Run the E2E publication validation script:
   ```bash
   bash scripts/test-e2e-publication.sh netsec-skills
   ```
   All checks must pass. If any fail, diagnose and fix the issue before proceeding.

2. Run the full BATS test suite:
   ```bash
   ./tests/bats/bin/bats tests/ --timing
   ```
   Expected: 467+ tests pass. The 6 known pre-existing validate-plugin-boundary.sh failures are acceptable. Zero new failures.

3. Run the boundary validation directly:
   ```bash
   bash scripts/validate-plugin-boundary.sh netsec-skills
   ```

4. Verify JSON validity of all manifest files:
   ```bash
   jq . .claude-plugin/marketplace.json
   jq . netsec-skills/.claude-plugin/plugin.json
   jq . netsec-skills/marketplace.json
   jq . netsec-skills/hooks/hooks.json
   ```

5. Verify repo-root marketplace points to plugin:
   ```bash
   jq -e '.plugins[0].source == "./netsec-skills"' .claude-plugin/marketplace.json
   ```

6. Count final artifacts:
   - SKILL.md files in plugin: `find netsec-skills/skills -name SKILL.md | wc -l` (expect 31)
   - Symlinks in plugin: `find netsec-skills -type l | wc -l` (expect 0)
   - Agent files: `ls netsec-skills/agents/*.md | wc -l` (expect 3)

If ANY automated check fails, fix the issue and re-run. Do not proceed to checkpoint until all automated checks pass.
  </action>
  <verify>
    <automated>bash scripts/test-e2e-publication.sh netsec-skills && ./tests/bats/bin/bats tests/ --timing</automated>
  </verify>
  <done>All automated validation passes: E2E script green, BATS suite green (467+ pass), boundary check clean, all JSON valid, correct artifact counts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete netsec-skills plugin validated by automated tests. Ready for manual smoke test with real Claude Code plugin loading. All structural, boundary, and regression tests pass.
  </what-built>
  <how-to-verify>
1. Load the plugin locally:
   ```bash
   claude --plugin-dir ./netsec-skills
   ```

2. Inside the Claude Code session, verify skills are discoverable:
   - Type `/netsec-skills:netsec-health` -- should run health check
   - Type `/netsec-skills:check-tools` -- should list tool availability
   - Type `/netsec-skills:scope` -- should show scope management options

3. Verify hooks are registered:
   - Check that PreToolUse and PostToolUse hooks appear in hook list
   - The safety banner should activate when tool commands are attempted

4. (Optional) Test skills.sh discovery:
   ```bash
   npx skills add PatrykQuantumNomad/networking-tools --list
   ```
   This shows what skills.sh would install. Check for duplicates (should show ~27-31 skills, not 54+).

5. Confirm you are satisfied the plugin is publication-ready.
  </how-to-verify>
  <resume-signal>Type "approved" to confirm publication readiness, or describe any issues found during manual testing.</resume-signal>
</task>

</tasks>

<verification>
1. E2E script: `bash scripts/test-e2e-publication.sh netsec-skills` all pass
2. BATS suite: `./tests/bats/bin/bats tests/ --timing` 467+ pass, zero new failures
3. JSON validity: all 4 manifest files parse cleanly with jq
4. Human confirmation: plugin loads and skills respond in real Claude Code session
</verification>

<success_criteria>
- All automated tests pass (E2E + BATS + boundary)
- Plugin loads via `claude --plugin-dir ./netsec-skills` without errors
- At least one skill responds correctly when triggered in the live session
- User approves publication readiness
</success_criteria>

<output>
After completion, create `.planning/phases/39-end-to-end-testing-and-publication/39-02-SUMMARY.md`
</output>
