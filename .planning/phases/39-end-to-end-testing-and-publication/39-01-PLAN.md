---
phase: 39-end-to-end-testing-and-publication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/marketplace.json
  - scripts/test-e2e-publication.sh
  - netsec-skills/README.md
autonomous: true
requirements: [PLUG-04, PUBL-01, PUBL-02, PUBL-03]

must_haves:
  truths:
    - "Repo-root .claude-plugin/marketplace.json exists and points to netsec-skills/ as plugin source"
    - "E2E validation script passes all structural checks (plugin.json, 31 SKILL.md files, hooks executable, 3 agents, zero symlinks, GSD boundary clean)"
    - "README documents both installation channels with clear guidance on what each provides"
  artifacts:
    - path: ".claude-plugin/marketplace.json"
      provides: "Plugin marketplace catalog at repo root for distribution"
      contains: "netsec-skills"
    - path: "scripts/test-e2e-publication.sh"
      provides: "Comprehensive E2E publication validation"
      min_lines: 60
    - path: "netsec-skills/README.md"
      provides: "Two-channel installation documentation"
      contains: "plugin marketplace"
  key_links:
    - from: ".claude-plugin/marketplace.json"
      to: "netsec-skills/.claude-plugin/plugin.json"
      via: "source field pointing to ./netsec-skills"
      pattern: '"source".*\\./netsec-skills'
    - from: "scripts/test-e2e-publication.sh"
      to: "scripts/validate-plugin-boundary.sh"
      via: "GSD boundary check delegation"
      pattern: "validate-plugin-boundary"
---

<objective>
Create the repo-root marketplace catalog, comprehensive E2E validation script, and updated README for two-channel distribution.

Purpose: These are the Wave 0 gaps identified in research -- the missing artifacts needed before the plugin can be published. The marketplace.json at repo root enables plugin marketplace distribution. The E2E script validates the entire plugin is publication-ready. The README guides users to choose the right installation channel.

Output: .claude-plugin/marketplace.json, scripts/test-e2e-publication.sh, updated netsec-skills/README.md
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-end-to-end-testing-and-publication/39-RESEARCH.md
@netsec-skills/.claude-plugin/plugin.json
@netsec-skills/marketplace.json
@netsec-skills/README.md

<interfaces>
<!-- Existing plugin manifest (netsec-skills/.claude-plugin/plugin.json): -->
{
  "name": "netsec-skills",
  "version": "1.0.0",
  "description": "Pentesting skills pack -- 17 tool skills, 6 workflows, 3 agent personas..."
}

<!-- Existing content catalog (netsec-skills/marketplace.json): -->
27 skills (17 tool + 6 workflow + 4 utility), 2 hooks, 3 agents

<!-- Existing boundary validation script: -->
scripts/validate-plugin-boundary.sh -- checks for GSD leakage in plugin directory

<!-- Plugin directory structure: -->
netsec-skills/
  .claude-plugin/plugin.json
  skills/ (31 SKILL.md files: 17 tools + 6 workflows + 3 agent invokers + 5 utility)
  agents/ (3 .md files: pentester, defender, analyst)
  hooks/ (hooks.json, netsec-pretool.sh, netsec-posttool.sh, netsec-health.sh)
  scripts/ (netsec-scope.sh)
  marketplace.json (content catalog)
  README.md

<!-- Note: marketplace.json lists 27 skills (excludes 3 agent invoker skills and pentest-conventions) -->
<!-- Actual SKILL.md count is 31 (27 listed + 3 agent invokers + pentest-conventions) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create repo-root marketplace.json and E2E validation script</name>
  <files>.claude-plugin/marketplace.json, scripts/test-e2e-publication.sh</files>
  <action>
1. Create directory `.claude-plugin/` at repo root (NOT inside netsec-skills/).

2. Create `.claude-plugin/marketplace.json` with this exact structure:
```json
{
  "name": "netsec-tools",
  "owner": {
    "name": "PatrykQuantumNomad",
    "url": "https://github.com/PatrykQuantumNomad"
  },
  "metadata": {
    "description": "Pentesting skills pack for Claude Code -- 17 tool skills, 6 workflows, 3 agent personas for network security testing"
  },
  "plugins": [
    {
      "name": "netsec-skills",
      "source": "./netsec-skills",
      "description": "17 tool skills, 6 workflows, 3 agent personas for network security testing",
      "version": "1.0.0",
      "homepage": "https://github.com/PatrykQuantumNomad/networking-tools",
      "repository": "https://github.com/PatrykQuantumNomad/networking-tools",
      "license": "MIT",
      "keywords": ["pentesting", "security", "nmap", "sqlmap", "hashcat", "metasploit", "red-team", "CTF"],
      "category": "security"
    }
  ]
}
```

This is the REPO-ROOT marketplace catalog (different from netsec-skills/marketplace.json which is the content catalog). The `source` field points to the plugin subdirectory.

3. Create `scripts/test-e2e-publication.sh` -- a comprehensive E2E validation script. Follow the project's script pattern (shebang, description comment, set -euo pipefail). Structure:

```bash
#!/usr/bin/env bash
# scripts/test-e2e-publication.sh -- End-to-end plugin publication validation
# Validates the netsec-skills plugin directory is ready for publication
# via both skills.sh and plugin marketplace channels.
#
# Usage: bash scripts/test-e2e-publication.sh [plugin-dir]
# Default plugin-dir: netsec-skills

set -euo pipefail

PLUGIN_DIR="${1:-netsec-skills}"
PASS=0
FAIL=0
TOTAL=0
ERRORS=""

# Colors (respect NO_COLOR)
if [[ -z "${NO_COLOR:-}" ]]; then
    GREEN=$'\033[32m'; RED=$'\033[31m'; YELLOW=$'\033[33m'; RESET=$'\033[0m'
else
    GREEN=""; RED=""; YELLOW=""; RESET=""
fi

_check() {
    local desc="$1"; shift
    TOTAL=$((TOTAL + 1))
    if "$@" > /dev/null 2>&1; then
        printf "  ${GREEN}PASS${RESET}: %s\n" "$desc"
        PASS=$((PASS + 1))
    else
        printf "  ${RED}FAIL${RESET}: %s\n" "$desc"
        FAIL=$((FAIL + 1))
        ERRORS="${ERRORS}\n  - ${desc}"
    fi
}
```

Then implement these test sections (each with echo header):

**Section 1: Plugin Structure**
- plugin.json exists at $PLUGIN_DIR/.claude-plugin/plugin.json
- plugin.json is valid JSON (jq . file)
- marketplace.json exists at $PLUGIN_DIR/marketplace.json
- marketplace.json is valid JSON
- hooks.json exists at $PLUGIN_DIR/hooks/hooks.json
- hooks.json is valid JSON
- Repo-root marketplace exists at .claude-plugin/marketplace.json
- Repo-root marketplace is valid JSON
- Repo-root marketplace source points to plugin dir (jq -e '.plugins[0].source == "./netsec-skills"')

**Section 2: Skills**
- Count SKILL.md files (expect 31: 17 tools + 6 workflows + 3 agent invokers + 5 utility)
- Every SKILL.md has `name:` field in frontmatter (use awk to extract between --- markers, grep for `^name:`)
- Every SKILL.md has `description:` field in frontmatter
- marketplace.json skill count matches expected (27 entries -- jq '.skills | length')

**Section 3: Hooks**
- netsec-pretool.sh exists and is executable (test -x)
- netsec-posttool.sh exists and is executable (test -x)
- netsec-health.sh exists

**Section 4: Agents**
- 3 agent .md files in agents/ directory
- pentester.md exists
- defender.md exists
- analyst.md exists

**Section 5: GSD Boundary**
- No files with gsd- prefix in plugin dir (find $PLUGIN_DIR -name 'gsd-*' | wc -l equals 0)
- No .planning directory in plugin dir
- Run validate-plugin-boundary.sh if it exists (bash scripts/validate-plugin-boundary.sh "$PLUGIN_DIR")

**Section 6: Portability**
- Zero symlinks in plugin dir (find $PLUGIN_DIR -type l | wc -l equals 0)
- No absolute paths in hook scripts (grep -r "^/" in .sh files under hooks/, excluding shebangs)

**Summary section:**
```bash
echo ""
echo "=== Results: ${PASS}/${TOTAL} passed, ${FAIL} failed ==="
if [[ "$FAIL" -gt 0 ]]; then
    printf "\nFailed checks:${ERRORS}\n"
    exit 1
fi
exit 0
```

Make the script executable: chmod +x scripts/test-e2e-publication.sh
  </action>
  <verify>
    <automated>bash scripts/test-e2e-publication.sh netsec-skills</automated>
  </verify>
  <done>E2E script runs and all checks pass. Repo-root .claude-plugin/marketplace.json exists with valid JSON pointing source to ./netsec-skills.</done>
</task>

<task type="auto">
  <name>Task 2: Update README with two-channel documentation</name>
  <files>netsec-skills/README.md</files>
  <action>
Rewrite netsec-skills/README.md to clearly document both distribution channels. The current README mentions skills.sh briefly but does not explain the channel differences.

Keep the existing content structure (skills tables, agent descriptions, safety section) but update the installation section to clearly present both channels with what each provides:

**Installation section must cover:**

1. **Plugin Marketplace (recommended for full functionality):**
   - Command: `/plugin marketplace add PatrykQuantumNomad/networking-tools` then `/plugin install netsec-skills@netsec-tools`
   - Provides: All 27 skills + safety hooks + 3 agent personas
   - Skills namespaced as `netsec-skills:skill-name` (e.g., `/netsec-skills:nmap`)

2. **skills.sh (lightweight, skills only):**
   - Command: `npx skills add PatrykQuantumNomad/networking-tools`
   - Provides: Individual SKILL.md files only (no hooks, no agents)
   - Skills accessed directly as `/nmap`, `/recon`, etc.
   - Note: Safety hooks and agent personas are NOT included -- users who want full safety infrastructure should use the plugin channel

3. **Local development:**
   - Command: `claude --plugin-dir ./netsec-skills`
   - For contributors and local testing

**Add a "Channel Comparison" section** as a concise table:

| Feature | Plugin Marketplace | skills.sh |
|---------|-------------------|-----------|
| Tool skills (17) | Yes | Yes |
| Workflow skills (6) | Yes | Yes |
| Utility skills (4) | Yes | Yes |
| Safety hooks | Yes | No |
| Agent personas | Yes | No |
| Scope management | Yes | Partial (skill only, no hooks) |
| Namespace | /netsec-skills:name | /name |

Keep existing Quick Start, Skills tables, Agent Personas, Safety, and Requirements sections. Update skill trigger examples in Quick Start to show BOTH namespaced (plugin) and flat (skills.sh) formats.

Do NOT change skill descriptions or counts. Do NOT add emojis.
  </action>
  <verify>
    <automated>bash scripts/test-e2e-publication.sh netsec-skills</automated>
  </verify>
  <done>README clearly documents both channels with comparison table. E2E validation still passes after README update.</done>
</task>

</tasks>

<verification>
1. `bash scripts/test-e2e-publication.sh netsec-skills` passes all checks
2. `jq . .claude-plugin/marketplace.json` produces valid output
3. `jq -e '.plugins[0].source == "./netsec-skills"' .claude-plugin/marketplace.json` returns true
4. netsec-skills/README.md contains both "plugin marketplace" and "skills.sh" installation instructions
</verification>

<success_criteria>
- Repo-root .claude-plugin/marketplace.json exists and is valid JSON with source pointing to ./netsec-skills
- E2E validation script exists at scripts/test-e2e-publication.sh, is executable, and all checks pass
- README documents both installation channels with channel comparison table
- Zero regressions: existing BATS tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/39-end-to-end-testing-and-publication/39-01-SUMMARY.md`
</output>
