---
phase: 36-dual-mode-tool-skills
plan: 02
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - .claude/skills/nmap/SKILL.md
  - .claude/skills/tshark/SKILL.md
  - .claude/skills/metasploit/SKILL.md
  - .claude/skills/aircrack-ng/SKILL.md
  - .claude/skills/hashcat/SKILL.md
  - .claude/skills/skipfish/SKILL.md
  - .claude/skills/sqlmap/SKILL.md
  - .claude/skills/hping3/SKILL.md
  - .claude/skills/john/SKILL.md
  - .claude/skills/nikto/SKILL.md
  - .claude/skills/foremost/SKILL.md
  - .claude/skills/traceroute/SKILL.md
  - .claude/skills/gobuster/SKILL.md
  - .claude/skills/ffuf/SKILL.md
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-03, TOOL-04]

must_haves:
  truths:
    - "All 14 remaining tool skills produce direct tool commands in standalone mode"
    - "All 14 remaining tool skills reference wrapper scripts with -j -x in in-repo mode"
    - "All 14 remaining tool skills show tool install status with platform-specific guidance"
    - "All 14 remaining tool skill descriptions use task-oriented keywords without 'wrapper scripts'"
  artifacts:
    - path: ".claude/skills/nmap/SKILL.md"
      provides: "Dual-mode nmap skill"
      contains: "command -v nmap"
    - path: ".claude/skills/tshark/SKILL.md"
      provides: "Dual-mode tshark skill"
      contains: "command -v tshark"
    - path: ".claude/skills/metasploit/SKILL.md"
      provides: "Dual-mode metasploit skill"
      contains: "command -v msfconsole"
    - path: ".claude/skills/aircrack-ng/SKILL.md"
      provides: "Dual-mode aircrack-ng skill"
      contains: "command -v aircrack-ng"
    - path: ".claude/skills/hashcat/SKILL.md"
      provides: "Dual-mode hashcat skill"
      contains: "command -v hashcat"
    - path: ".claude/skills/sqlmap/SKILL.md"
      provides: "Dual-mode sqlmap skill"
      contains: "command -v sqlmap"
    - path: ".claude/skills/gobuster/SKILL.md"
      provides: "Dual-mode gobuster skill"
      contains: "command -v gobuster"
    - path: ".claude/skills/ffuf/SKILL.md"
      provides: "Dual-mode ffuf skill"
      contains: "command -v ffuf"
  key_links:
    - from: "tests/test-dual-mode-skills.bats"
      to: ".claude/skills/*/SKILL.md"
      via: "structural validation of all 17 skills"
      pattern: "Mode: Standalone|Mode: Wrapper"
---

<objective>
Transform remaining 14 tool skills to dual-mode format using the validated pilot pattern from Plan 01. Each skill gets inline command knowledge, wrapper detection, install detection, and keyword-optimized description.

Purpose: Scale the proven dual-mode pattern from 3 pilot tools to all 17 tools, completing TOOL-01 through TOOL-04 for every tool.
Output: 14 transformed SKILL.md files, all 17 tools passing BATS structural validation
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-dual-mode-tool-skills/36-RESEARCH.md
@.planning/phases/36-dual-mode-tool-skills/36-01-SUMMARY.md

# Pilot pattern to replicate (use dig as the template)
@.claude/skills/dig/SKILL.md

# Install hints and binary names
@scripts/check-tools.sh

# Test scaffold (all 17 tools must pass)
@tests/test-dual-mode-skills.bats
</context>

<interfaces>
<!-- Validated dual-mode pattern from Plan 01 pilot (dig/curl/netcat) -->
<!-- ALL 14 remaining skills MUST follow this exact structure -->

Required SKILL.md sections (in order):
1. Frontmatter: name, description (under 200 chars, action verbs, no "wrapper scripts"), disable-model-invocation: true
2. `# <Tool Name>` heading
3. `## Tool Status` with dynamic injection lines
4. `## Mode: Wrapper Scripts Available` with script references + -j -x
5. `## Mode: Standalone (Direct Commands)` with inline tool commands
6. `## Defaults`
7. `## Target Validation`

Tool-to-binary mapping (from check-tools.sh):
- nmap -> nmap
- tshark -> tshark
- metasploit -> msfconsole (version: grep metasploit /opt/metasploit-framework/version-manifest.txt 2>/dev/null | head -1)
- aircrack-ng -> aircrack-ng
- hashcat -> hashcat
- skipfish -> skipfish
- sqlmap -> sqlmap
- hping3 -> hping3
- john -> john
- nikto -> nikto (version: nikto -Version 2>/dev/null | head -1)
- foremost -> foremost
- traceroute -> traceroute (version: echo "installed" -- no --version on macOS)
- gobuster -> gobuster (version: gobuster version 2>/dev/null | head -1)
- ffuf -> ffuf (version: ffuf -V 2>&1 | head -1)

Install hints (from check-tools.sh TOOLS array):
- nmap: brew install nmap
- tshark: brew install wireshark (includes tshark CLI)
- msfconsole: https://docs.metasploit.com/docs/using-metasploit/getting-started/nightly-installers.html
- aircrack-ng: brew install aircrack-ng
- hashcat: brew install hashcat
- skipfish: sudo port install skipfish
- sqlmap: brew install sqlmap
- hping3: brew install draftbrew/tap/hping
- john: brew install john-jumbo
- nikto: brew install nikto
- foremost: brew install foremost
- traceroute: pre-installed on macOS | apt install traceroute (Debian/Ubuntu)
- gobuster: brew install gobuster (or: go install github.com/OJ/gobuster/v3@latest)
- ffuf: brew install ffuf (or: go install github.com/ffuf/ffuf/v2@latest)

Recommended descriptions (from 36-RESEARCH.md):
- nmap: Scan networks, discover hosts, detect open ports and services with nmap. Port scanning, host discovery, OS detection, service enumeration, NSE scripts.
- tshark: Capture and analyze network traffic with tshark. Packet capture, protocol analysis, display filters, credential extraction, file carving from pcaps.
- metasploit: Generate payloads, scan services, and set up listeners with Metasploit. Reverse shells, msfvenom, auxiliary scanners, multi/handler.
- aircrack-ng: Audit WiFi security and crack WPA handshakes with aircrack-ng. Wireless scanning, monitor mode, handshake capture, dictionary attacks.
- hashcat: Crack password hashes with GPU acceleration using hashcat. NTLM, MD5, SHA-256, bcrypt, rule-based attacks, benchmarking.
- skipfish: Scan web applications for vulnerabilities with skipfish. Web crawler, authenticated scanning, security assessment.
- sqlmap: Detect SQL injection and extract databases with sqlmap. Parameter testing, WAF bypass, database enumeration, tamper scripts.
- hping3: Craft packets and test firewalls with hping3. TCP flag probes, firewall detection, custom ICMP/UDP/TCP packets.
- john: Crack passwords and identify hash types with John the Ripper. Linux shadows, archive passwords, wordlists, incremental mode.
- nikto: Scan web servers for vulnerabilities with nikto. CGI checks, outdated software, misconfigurations, authenticated scanning.
- foremost: Recover deleted files and carve data from disk images with foremost. File carving, forensic analysis, header/footer recovery.
- traceroute: Trace network paths and diagnose latency with traceroute and mtr. Hop analysis, route comparison, real-time latency monitoring.
- gobuster: Discover hidden directories and enumerate subdomains with gobuster. Directory brute-force, DNS enumeration, wordlist scanning.
- ffuf: Fuzz web parameters, directories, and endpoints with ffuf. Parameter discovery, content filtering, custom wordlists.

Wrapper script inventory (from 36-RESEARCH.md table):
- nmap: discover-live-hosts, identify-ports, scan-web-vulnerabilities
- tshark: capture-http-credentials, analyze-dns-queries, extract-files-from-capture
- metasploit: generate-reverse-shell, scan-network-services, setup-listener
- aircrack-ng: analyze-wireless-networks, capture-handshake, crack-wpa-handshake
- hashcat: benchmark-gpu, crack-ntlm-hashes, crack-web-hashes
- skipfish: quick-scan-web-app, scan-authenticated-app
- sqlmap: dump-database, test-all-parameters, bypass-waf
- hping3: detect-firewall, test-firewall-rules
- john: crack-linux-passwords, crack-archive-passwords, identify-hash-type
- nikto: scan-specific-vulnerabilities, scan-multiple-hosts, scan-with-auth
- foremost: recover-deleted-files, carve-specific-filetypes, analyze-forensic-image
- traceroute: trace-network-path, compare-routes, diagnose-latency
- gobuster: discover-directories, enumerate-subdomains
- ffuf: fuzz-parameters
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Transform first 7 tools (nmap, tshark, metasploit, aircrack-ng, hashcat, skipfish, sqlmap)</name>
  <files>.claude/skills/nmap/SKILL.md, .claude/skills/tshark/SKILL.md, .claude/skills/metasploit/SKILL.md, .claude/skills/aircrack-ng/SKILL.md, .claude/skills/hashcat/SKILL.md, .claude/skills/skipfish/SKILL.md, .claude/skills/sqlmap/SKILL.md</files>
  <action>
For each of the 7 tools, rewrite SKILL.md from wrapper-only to dual-mode format following the exact pattern established by the dig pilot in Plan 01.

For each tool:
1. Read the current SKILL.md to understand wrapper script references
2. Read the corresponding wrapper scripts (`scripts/<tool>/*.sh`) to extract `run_or_show` commands as inline knowledge
3. Rewrite SKILL.md with all required sections (Tool Status, Wrapper Mode, Standalone Mode, Defaults, Target Validation)

**Per-tool inline knowledge to extract:**

**nmap** (3 scripts): Host discovery (nmap -sn, -PS, -PA, -PE), port scanning (nmap -sS, -sT, -p-, -sV, -O), web vuln scanning (nmap --script=http-enum, --script=http-vuln-*, --script=ssl-enum-ciphers), useful combos (nmap -A, -sS -sV -O -T4). Representative script for detection: identify-ports.sh

**tshark** (3 scripts): Live capture (tshark -i eth0 -c 100, tshark -f "port 80"), display filters (tshark -r file.pcap -Y "http.request"), credential extraction (tshark -Y "http.request.method==POST" -T fields -e http.file_data), DNS analysis (tshark -Y "dns" -T fields -e dns.qry.name), file extraction (tshark --export-objects). Representative script for detection: capture-http-credentials.sh

**metasploit** (3 scripts): Payload generation (msfvenom -p <payload> LHOST=<ip> LPORT=<port> -f <format>), service scanning (msfconsole -q -x "use auxiliary/scanner/..."), listener setup (msfconsole -q -x "use exploit/multi/handler; set PAYLOAD ..."). Representative script for detection: scan-network-services.sh. Note: binary is `msfconsole`, not `metasploit`.

**aircrack-ng** (3 scripts): Wireless scanning (airodump-ng <iface>), monitor mode (airmon-ng start <iface>), handshake capture (airodump-ng --bssid <mac> -c <ch> -w capture), cracking (aircrack-ng -w wordlist capture.cap). Representative script for detection: analyze-wireless-networks.sh

**hashcat** (3 scripts): Benchmarking (hashcat -b), NTLM cracking (hashcat -m 1000 -a 0 hashes.txt wordlist.txt), web hash cracking (hashcat -m 3200 -a 0 for bcrypt, -m 0 for MD5), rule-based (hashcat -r rules/best64.rule). Representative script for detection: crack-ntlm-hashes.sh. Note: hashcat is an offline tool -- no network target needed.

**skipfish** (2 scripts): Quick scan (skipfish -o output_dir http://<target>), authenticated scan (skipfish -o output_dir -C "cookie=value" http://<target>). Representative script for detection: quick-scan-web-app.sh

**sqlmap** (3 scripts): Basic test (sqlmap -u "http://<target>/page?id=1" --batch), all parameters (sqlmap -u <url> --forms --crawl=2 --batch), WAF bypass (sqlmap -u <url> --tamper=space2comment --random-agent), database dump (sqlmap -u <url> -D <db> --dump). Representative script for detection: test-all-parameters.sh

Each skill should be 80-150 lines. Focus on the most useful 6-10 commands per tool. Include brief category context (2-3 lines per section explaining when/why).

Use the description text from the interfaces block above. Use install hints from check-tools.sh.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/networking-tools && ./tests/bats/bin/bats tests/test-dual-mode-skills.bats --timing 2>&1 | grep -E "(ok|not ok)" | head -30</automated>
  </verify>
  <done>7 additional tools pass all BATS structural tests (TOOL-01 through TOOL-04). Total: 10 of 17 tools passing. Each SKILL.md has standalone commands, wrapper references, install detection, and optimized description.</done>
</task>

<task type="auto">
  <name>Task 2: Transform remaining 7 tools (hping3, john, nikto, foremost, traceroute, gobuster, ffuf)</name>
  <files>.claude/skills/hping3/SKILL.md, .claude/skills/john/SKILL.md, .claude/skills/nikto/SKILL.md, .claude/skills/foremost/SKILL.md, .claude/skills/traceroute/SKILL.md, .claude/skills/gobuster/SKILL.md, .claude/skills/ffuf/SKILL.md</files>
  <action>
For each of the remaining 7 tools, rewrite SKILL.md following the same dual-mode pattern. Read each tool's wrapper scripts to extract inline knowledge.

**Per-tool inline knowledge to extract:**

**hping3** (2 scripts): Firewall detection (hping3 -S -p 80 <target>, hping3 -A -p 80 <target>), firewall rule testing (hping3 -S -p <port> <target>, hping3 --icmp <target>, hping3 -2 -p <port> <target> for UDP). Representative script for detection: detect-firewall.sh

**john** (3 scripts): Linux passwords (unshadow /etc/passwd /etc/shadow > combined.txt; john combined.txt), archive cracking (zip2john file.zip > hash.txt; john hash.txt), hash identification (john --list=formats | grep -i <type>), wordlist mode (john --wordlist=wordlist.txt hash.txt), incremental (john --incremental hash.txt). Representative script for detection: crack-linux-passwords.sh. Note: offline tool.

**nikto** (3 scripts): Basic scan (nikto -h http://<target>), specific vulns (nikto -h <target> -Tuning <code>), multiple hosts (nikto -h hosts.txt), authenticated scan (nikto -h <target> -id user:pass, nikto -h <target> -C "cookie=value"). Representative script for detection: scan-specific-vulnerabilities.sh

**foremost** (3 scripts): File recovery (foremost -i image.dd -o output/), specific types (foremost -t jpg,png,pdf -i image.dd -o output/), forensic image (foremost -i evidence.E01 -o output/ -v). Representative script for detection: recover-deleted-files.sh. Note: offline/file-based tool.

**traceroute** (3 scripts + mtr): Path tracing (traceroute <target>, traceroute -T <target> for TCP, traceroute -I <target> for ICMP), route comparison (mtr --report <target>, mtr -r -c 10 <target>), latency diagnosis (mtr -r -c 20 <target>, traceroute -n <target>). Representative script for detection: trace-network-path.sh. Note: skill covers both traceroute AND mtr; check both binaries.

**gobuster** (2 scripts): Directory discovery (gobuster dir -u http://<target> -w wordlist.txt -t 10), subdomain enumeration (gobuster dns -d <domain> -w subdomains.txt -t 10). Representative script for detection: discover-directories.sh. Note: requires wordlist files.

**ffuf** (1 script): Parameter fuzzing (ffuf -u http://<target>/FUZZ -w wordlist.txt), filtered fuzzing (ffuf -u <url>/FUZZ -w wordlist.txt -fc 404), POST fuzzing (ffuf -u <url> -X POST -d "param=FUZZ" -w wordlist.txt). Representative script for detection: fuzz-parameters.sh. Note: requires wordlist files.

Same rules as Task 1: 80-150 lines per skill, use descriptions from interfaces block, use install hints from check-tools.sh.

Special notes:
- **foremost, john, hashcat** are offline tools -- their Target Validation section should note they work on local files, not network targets
- **traceroute** skill covers two tools (traceroute + mtr) -- check both binaries in Tool Status
- **gobuster, ffuf** -- mention wordlist requirements (SecLists recommended)
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/networking-tools && ./tests/bats/bin/bats tests/test-dual-mode-skills.bats --timing 2>&1 | tail -5</automated>
  </verify>
  <done>All 17 tool skills pass BATS structural validation for TOOL-01 through TOOL-04. Every skill has standalone commands, wrapper references, install detection, and optimized description. Zero test failures (excluding sync test which awaits Plan 03).</done>
</task>

</tasks>

<verification>
1. `./tests/bats/bin/bats tests/test-dual-mode-skills.bats` -- all 17 tools pass structural tests
2. Every `.claude/skills/<tool>/SKILL.md` contains both `## Mode: Standalone` and `## Mode: Wrapper Scripts Available`
3. No SKILL.md description contains "wrapper scripts"
4. All descriptions under 200 characters
</verification>

<success_criteria>
- All 17 tool skills transformed to dual-mode format
- All BATS structural tests pass for TOOL-01 through TOOL-04
- Each skill has inline commands matching its wrapper script capabilities
- Pattern is consistent across all 17 tools
</success_criteria>

<output>
After completion, create `.planning/phases/36-dual-mode-tool-skills/36-02-SUMMARY.md`
</output>
