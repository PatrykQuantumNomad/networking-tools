---
phase: 36-dual-mode-tool-skills
plan: 03
type: execute
wave: 3
depends_on: [36-02]
files_modified:
  - netsec-skills/skills/tools/aircrack-ng/SKILL.md
  - netsec-skills/skills/tools/curl/SKILL.md
  - netsec-skills/skills/tools/dig/SKILL.md
  - netsec-skills/skills/tools/ffuf/SKILL.md
  - netsec-skills/skills/tools/foremost/SKILL.md
  - netsec-skills/skills/tools/gobuster/SKILL.md
  - netsec-skills/skills/tools/hashcat/SKILL.md
  - netsec-skills/skills/tools/hping3/SKILL.md
  - netsec-skills/skills/tools/john/SKILL.md
  - netsec-skills/skills/tools/metasploit/SKILL.md
  - netsec-skills/skills/tools/netcat/SKILL.md
  - netsec-skills/skills/tools/nikto/SKILL.md
  - netsec-skills/skills/tools/nmap/SKILL.md
  - netsec-skills/skills/tools/skipfish/SKILL.md
  - netsec-skills/skills/tools/sqlmap/SKILL.md
  - netsec-skills/skills/tools/traceroute/SKILL.md
  - netsec-skills/skills/tools/tshark/SKILL.md
  - netsec-skills/marketplace.json
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-03, TOOL-04]

must_haves:
  truths:
    - "Plugin tool skills are real files (not symlinks) identical to in-repo skills"
    - "marketplace.json descriptions match the new SKILL.md descriptions for all 17 tools"
    - "All 17 skills pass full BATS validation including the sync test"
    - "Full BATS test suite (existing 435 + new dual-mode tests) passes without regressions"
  artifacts:
    - path: "netsec-skills/skills/tools/nmap/SKILL.md"
      provides: "Plugin copy of dual-mode nmap skill (real file, not symlink)"
      contains: "Mode: Standalone"
    - path: "netsec-skills/marketplace.json"
      provides: "Updated descriptions matching new SKILL.md keywords"
      contains: "Scan networks, discover hosts"
  key_links:
    - from: "netsec-skills/skills/tools/*/SKILL.md"
      to: ".claude/skills/*/SKILL.md"
      via: "identical content (diff returns empty)"
      pattern: "diff.*returns empty"
    - from: "netsec-skills/marketplace.json"
      to: ".claude/skills/*/SKILL.md"
      via: "description field matches frontmatter description"
      pattern: "description.*Scan|Capture|Crack"
---

<objective>
Replace plugin symlinks with real copies of the dual-mode skills, update marketplace.json descriptions to match the new keyword-optimized descriptions, and run the full test suite to confirm zero regressions.

Purpose: Ensure the plugin distribution has standalone files (not symlinks that break outside the repo) with discoverable descriptions, completing the dual-mode transformation.
Output: 17 real SKILL.md files in plugin dir, updated marketplace.json, full test suite green
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-dual-mode-tool-skills/36-RESEARCH.md
@.planning/phases/36-dual-mode-tool-skills/36-02-SUMMARY.md

# Source of truth for descriptions
@.claude/skills/dig/SKILL.md

# Current marketplace catalog
@netsec-skills/marketplace.json

# Boundary validation script (run after changes)
@scripts/validate-plugin-boundary.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace plugin symlinks with real files and sync marketplace.json</name>
  <files>netsec-skills/skills/tools/aircrack-ng/SKILL.md, netsec-skills/skills/tools/curl/SKILL.md, netsec-skills/skills/tools/dig/SKILL.md, netsec-skills/skills/tools/ffuf/SKILL.md, netsec-skills/skills/tools/foremost/SKILL.md, netsec-skills/skills/tools/gobuster/SKILL.md, netsec-skills/skills/tools/hashcat/SKILL.md, netsec-skills/skills/tools/hping3/SKILL.md, netsec-skills/skills/tools/john/SKILL.md, netsec-skills/skills/tools/metasploit/SKILL.md, netsec-skills/skills/tools/netcat/SKILL.md, netsec-skills/skills/tools/nikto/SKILL.md, netsec-skills/skills/tools/nmap/SKILL.md, netsec-skills/skills/tools/skipfish/SKILL.md, netsec-skills/skills/tools/sqlmap/SKILL.md, netsec-skills/skills/tools/traceroute/SKILL.md, netsec-skills/skills/tools/tshark/SKILL.md, netsec-skills/marketplace.json</files>
  <action>
**Step 1: Replace symlinks with real files**

For each of the 17 tool skills in `netsec-skills/skills/tools/`:
1. Remove the symlink directory: `rm netsec-skills/skills/tools/<tool>`
2. Create a real directory: `mkdir -p netsec-skills/skills/tools/<tool>`
3. Copy the in-repo skill: `cp .claude/skills/<tool>/SKILL.md netsec-skills/skills/tools/<tool>/SKILL.md`

Do this for all 17 tools: nmap, tshark, metasploit, aircrack-ng, hashcat, skipfish, sqlmap, hping3, john, nikto, foremost, dig, curl, netcat, traceroute, gobuster, ffuf.

Verify after replacement: `file netsec-skills/skills/tools/*/SKILL.md` should show "ASCII text" not "symbolic link".

**Step 2: Update marketplace.json descriptions**

Read `netsec-skills/marketplace.json`. For each of the 17 tool skill entries, update the `description` field to match the new description from the corresponding `.claude/skills/<tool>/SKILL.md` frontmatter.

The description mapping (extracted from SKILL.md frontmatter after Plan 02):
- nmap: "Scan networks, discover hosts, detect open ports and services with nmap. Port scanning, host discovery, OS detection, service enumeration, NSE scripts."
- tshark: "Capture and analyze network traffic with tshark. Packet capture, protocol analysis, display filters, credential extraction, file carving from pcaps."
- metasploit: "Generate payloads, scan services, and set up listeners with Metasploit. Reverse shells, msfvenom, auxiliary scanners, multi/handler."
- aircrack-ng: "Audit WiFi security and crack WPA handshakes with aircrack-ng. Wireless scanning, monitor mode, handshake capture, dictionary attacks."
- hashcat: "Crack password hashes with GPU acceleration using hashcat. NTLM, MD5, SHA-256, bcrypt, rule-based attacks, benchmarking."
- skipfish: "Scan web applications for vulnerabilities with skipfish. Web crawler, authenticated scanning, security assessment."
- sqlmap: "Detect SQL injection and extract databases with sqlmap. Parameter testing, WAF bypass, database enumeration, tamper scripts."
- hping3: "Craft packets and test firewalls with hping3. TCP flag probes, firewall detection, custom ICMP/UDP/TCP packets."
- john: "Crack passwords and identify hash types with John the Ripper. Linux shadows, archive passwords, wordlists, incremental mode."
- nikto: "Scan web servers for vulnerabilities with nikto. CGI checks, outdated software, misconfigurations, authenticated scanning."
- foremost: "Recover deleted files and carve data from disk images with foremost. File carving, forensic analysis, header/footer recovery."
- dig: "Query DNS records and test zone transfers with dig. A, MX, NS, TXT records, AXFR, propagation checks, nameserver queries."
- curl: "Debug HTTP requests and inspect SSL certificates with curl. TLS versions, certificate chains, headers, response timing."
- netcat: "Scan ports, set up listeners, and transfer files with netcat. TCP/UDP connections, port scanning, reverse shells, file transfer."
- traceroute: "Trace network paths and diagnose latency with traceroute and mtr. Hop analysis, route comparison, real-time latency monitoring."
- gobuster: "Discover hidden directories and enumerate subdomains with gobuster. Directory brute-force, DNS enumeration, wordlist scanning."
- ffuf: "Fuzz web parameters, directories, and endpoints with ffuf. Parameter discovery, content filtering, custom wordlists."

Do NOT modify any other marketplace.json fields (name, type, trigger, tags, requires, hooks, agents).

Validate marketplace.json is valid JSON after editing: `jq . netsec-skills/marketplace.json > /dev/null`
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/networking-tools && file netsec-skills/skills/tools/nmap/SKILL.md netsec-skills/skills/tools/dig/SKILL.md netsec-skills/skills/tools/ffuf/SKILL.md | grep -c "ASCII text" && diff .claude/skills/nmap/SKILL.md netsec-skills/skills/tools/nmap/SKILL.md && jq '.skills | length' netsec-skills/marketplace.json</automated>
  </verify>
  <done>All 17 plugin tool skills are real files (not symlinks) identical to their in-repo counterparts. marketplace.json has updated descriptions matching SKILL.md frontmatter. JSON validates cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Run full validation suite and boundary check</name>
  <files></files>
  <action>
Run the complete validation suite to confirm zero regressions:

1. **Dual-mode structural tests:** `./tests/bats/bin/bats tests/test-dual-mode-skills.bats --timing` -- all tests pass including the sync test (plugin files now match in-repo files)

2. **Plugin boundary validation:** `bash scripts/validate-plugin-boundary.sh` -- confirms zero GSD leakage, all files on allowlist, valid JSON, no broken symlinks (symlinks replaced with real files means this check passes differently now -- it validates real files against the allowlist)

3. **Full BATS suite:** `./tests/bats/bin/bats tests/ --timing` -- all existing tests (435+) plus new dual-mode tests pass. Confirm no regressions in existing test suites.

4. **Spot-check content:** For 3 representative tools (nmap, sqlmap, traceroute), verify:
   - `grep -c "## Mode: Standalone" netsec-skills/skills/tools/<tool>/SKILL.md` returns 1
   - `grep "description:" netsec-skills/skills/tools/<tool>/SKILL.md` does not contain "wrapper scripts"
   - `diff .claude/skills/<tool>/SKILL.md netsec-skills/skills/tools/<tool>/SKILL.md` returns empty

If boundary validation fails because allowlist expects symlinks but now finds real directories, update the boundary validation script's allowlist pattern to accept both real directories and symlinks for `skills/tools/*` paths.

If any BATS test fails, diagnose and fix the specific failure. Do not modify passing tests.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/networking-tools && ./tests/bats/bin/bats tests/test-dual-mode-skills.bats --timing 2>&1 | tail -3 && ./tests/bats/bin/bats tests/ --timing 2>&1 | tail -3</automated>
  </verify>
  <done>Full BATS suite passes (existing + new tests). Dual-mode sync test passes (plugin = in-repo). Plugin boundary check passes. Zero regressions. Phase 36 complete.</done>
</task>

</tasks>

<verification>
1. `file netsec-skills/skills/tools/*/SKILL.md | grep -c "symbolic link"` returns 0 (no symlinks remain)
2. `diff .claude/skills/nmap/SKILL.md netsec-skills/skills/tools/nmap/SKILL.md` returns empty for all 17 tools
3. `jq -r '.skills[] | select(.type=="tool") | .description' netsec-skills/marketplace.json | grep -c "wrapper scripts"` returns 0
4. `./tests/bats/bin/bats tests/` -- full suite green
5. `bash scripts/validate-plugin-boundary.sh` -- zero violations
</verification>

<success_criteria>
- Plugin tool skills are portable real files matching in-repo skills
- marketplace.json descriptions are keyword-optimized and in sync
- Full test suite passes with zero regressions
- Plugin boundary validation passes
- Phase 36 dual-mode transformation complete for all 17 tools
</success_criteria>

<output>
After completion, create `.planning/phases/36-dual-mode-tool-skills/36-03-SUMMARY.md`
</output>
